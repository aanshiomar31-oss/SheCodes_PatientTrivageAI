# ==============================================================================
# PatientTriage.ai — Backend Dependencies
# ==============================================================================
# Pinned to specific versions for reproducible builds across dev machines,
# CI, and Docker. Bump deliberately, not via a bare `pip install -U`.
#
# Install with:
#   pip install -r requirements.txt
# ==============================================================================

# --- Web framework / API ----------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# --- Settings / validation ---------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Database / migrations ----------------------------------------------------
sqlalchemy==2.0.36
alembic==1.14.0

# --- Data engineering ---------------------------------------------------------
pandas==2.2.3
numpy==1.26.4

# --- Machine learning ----------------------------------------------------------
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0
catboost==1.2.7
shap==0.46.0
matplotlib==3.9.3
optuna==4.1.0
mlflow==2.19.0

# --- Testing --------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- Dev tooling ------------------------------------------------------------------
black==24.10.0
ruff==0.8.4
